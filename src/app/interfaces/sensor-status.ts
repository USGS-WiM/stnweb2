export interface SensorStatus {
    instrument_status_id: number;
    status_type_id: number;
    instrument_id: number;
    time_stamp: string;
    notes: string;
    time_zone: string;
    member_id: number;
}
